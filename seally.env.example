## Seally 专属配置。将需要的配置追加到 Seafile 部署目录原有的 .env。
## 本文件不重复定义 Seafile 的 COMPOSE_FILE、数据库、缓存地址、域名、时区或 JWT；
## seally.yml 会直接读取 Seafile .env 中已有的对应配置。
## 不要直接覆盖官方 .env，也不要把真实 .env 提交到 Git。

## 必填：填写发布页中的完整版本标签或 digest，禁止使用 latest。
SEALLY_IMAGE=

## Seally 独立密钥：执行 openssl rand -hex 32 生成。
## 上线后不可更换；不要与 JWT_PRIVATE_KEY 共用。
SM_SESSION_SECRET=
